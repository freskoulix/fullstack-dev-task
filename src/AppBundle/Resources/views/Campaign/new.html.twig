{% extends '::base.html.twig' %}

{% block body %}
    <h1>Campaign creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('campaign') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
